<resources>

    <!-- Base application theme. -->
    <style name="Theme.Pressball" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/pressballColor</item>
        <item name="android:textColorPrimary">@color/textColor</item>
        <item name="android:actionMenuTextAppearance">@style/Theme.Pressball.ActionMenuTextAppearance</item>
    </style>

    <style name="Theme.Pressball.ActionMenuTextAppearance" parent="Theme.AppCompat.NoActionBar"></style>
    
    <style name="NewsImage">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:scaleType">fitXY</item>
    </style>
</resources>
